# bastion

## Fixes

- macOS:
  - [update-sdl2](macos/update-sdl2/) — replace the bundled SDL 2.0.6 with a
    modern build (2.32.10); fixes controllers not being recognised. Note: the
    8BitDo Ultimate 2 Wireless works, but the game does not auto-switch to the
    gamepad control scheme for it — switch manually in the options (an Xbox
    controller auto-switches as expected).
